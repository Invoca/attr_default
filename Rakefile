#!/usr/bin/env rake
require "bundler/gem_tasks"
require 'appraisal'
require 'rubygems'
require 'bundler/setup'

task :default => :test

desc "Run unit tests."
task :test do
  ruby "test/attr_default_test.rb"
end
